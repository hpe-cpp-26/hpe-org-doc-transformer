[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
	"config",
	"fastmcp>=3.2.4",
	"httpx>=0.28.1",
	"pydantic>=2.13.3",
]

[tool.uv.sources]
config = { workspace = true }
