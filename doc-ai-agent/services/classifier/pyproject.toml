[project]
name = "classifier"
version = "0.1.0"
description = "Document classification service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
	"config",
]

[tool.uv.sources]
config = { workspace = true }
dependencies = [
    "types",
    "embedding",
    "db",
]
