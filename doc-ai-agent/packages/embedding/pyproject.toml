[project]
name = "embedding"
version = "0.1.0"
description = "Embedding generation using Ollama"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "config",
    "requests>=2.31.0,<3",
]

[build-system]
requires = ["uv_build>=0.10.12,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
config = { workspace = true }
